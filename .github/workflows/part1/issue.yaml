name: issue-workflow
on: 
  issues:
    types: [opened]

jobs:
  issue-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run : echo "Hello from issue workflow"
      - name: step2
        run : |
          echo "This is step 2 of issue workflow"
          echo "Multiple lines in a single step"