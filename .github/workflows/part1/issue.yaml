name: issue-workflow
on: 
  issues:
    types: [opened]

jobs:
  issue-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run : echo "Hello from issue workflow"
      - name: step2
        run : |
          echo "This is step 2 of issue workflow"
          echo "Multiple lines in a single step"

# - 버그, 기능 요청, 질문, 논의 등 추척 및 관리
# - 프로젝트의 버그나 기능 개발 등의 진행 상황 파악